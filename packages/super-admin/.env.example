# Super Admin Dashboard Environment Variables

# === CRITICAL SECURITY ===
# Generate a strong secret key: openssl rand -base64 32
SUPER_ADMIN_SECRET_KEY=your-256-bit-secret-key-here

# Comma-separated list of allowed IP addresses
SUPER_ADMIN_ALLOWED_IPS=127.0.0.1,::1

# JWT secret for session tokens
JWT_SECRET=your-jwt-secret-here

# Session expiry (in seconds, default 8 hours)
SESSION_EXPIRY=28800

# === DATABASE ===
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/fortistate_analytics?schema=public

# === REDIS (for caching) ===
REDIS_URL=redis://localhost:6379

# === EXTERNAL SERVICES ===
# Stripe API key (for revenue data)
STRIPE_SECRET_KEY=sk_test_...

# OpenAI API key (for AI cost tracking)
OPENAI_API_KEY=sk-...

# === FORTISTATE CORE ===
# FortiState API endpoint
FORTISTATE_API_URL=http://localhost:3000

# Inspector API endpoint
INSPECTOR_API_URL=http://localhost:9229

# === MONITORING ===
# Enable performance monitoring
ENABLE_MONITORING=true

# Log level (debug, info, warn, error)
LOG_LEVEL=info
